



export const getDesignTheme = (mode: string): object => {
  return {
    palette: {
      button: {
        main: "#fff"
      }
    },
    typography: {
      fontFamily: ["Poppins", "sans-serif"].join(","),
    },
    // selected: {},
  };
};
